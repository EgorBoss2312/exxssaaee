# Портал корпоративной базы знаний (прототип ВКР)

Веб-приложение: **чат с ИИ (RAG)**, **база знаний**, **авторизация**, **роли и ограничение доступа к документам**.

**Docker не обязателен.** По умолчанию используется **SQLite** (файл `backend/data/app.db`), эмбеддинги хранятся в JSON, поиск — в Python/NumPy.

---

## Сайт в интернете (простой вариант)

**Почему в браузере «Сервер API не отвечает»:** развёрнут **только** собранный фронт (статика), а **FastAPI не запущен** или nginx отдаёт файлы из `dist`, но **не проксирует** `/api` на Uvicorn. Вход идёт на `POST /api/auth/login` — без бэкенда запрос пустой или 404.

**Правильный прод:** один процесс с **Python + статикой из одного образа** (корневой `Dockerfile`: Vite → `dist` внутри образа, Uvicorn отдаёт и `/api`, и SPA). Тогда **`VITE_API_BASE_URL` не нужен** (запросы к API с того же origin).

### Вариант A — Render.com (рекомендуется)

1. Зайти на [render.com](https://render.com) и войти через **GitHub**.  
2. **New** → **Blueprint** → выбрать репозиторий с проектом.  
3. Render подхватит **`render.yaml` из корня репозитория** → нажать **Apply**.  
   Если в корне Git лежит папка **`edda-portal/`**, а `Dockerfile` внутри неё — используйте **`render.yaml` из корня монорепозитория** (в шаблоне проекта он рядом с папкой `edda-portal`, с полем **`rootDir: edda-portal`**). Иначе Render ищет `./Dockerfile` в корне репо и синхронизация падает.  
4. Дождаться окончания сборки и деплоя (первый раз может занять **много минут**: Docker, PyTorch).  
5. Открыть выданный URL вида `https://…onrender.com` → вход: **`admin@edda.local`** / **`Admin123!`**

На бесплатном тарифе сервис «засыпает» — первый запрос после паузы может идти **1–2 минуты**. Данные SQLite в контейнере при пересоздании сервиса могут обнуляться — для дипломного прототипа обычно достаточно.

**Лимит 512MB RAM на Free:** загрузка **PyTorch + sentence-transformers** при старте не помещается в память → ошибка вроде `Ran out of memory (used over 512MB)`. В **`render.yaml`** задано **`EDDA_USE_HASH_EMBEDDINGS=1`**: эмбеддинги считаются без ML (качество поиска по смыслу ниже, зато вход и приложение работают). Для полноценного семантического RAG на Render нужен тип инстанса **Standard (2GB)** и **удалите** переменную `EDDA_USE_HASH_EMBEDDINGS` (или поставьте `0`).

**Жёлтая плашка «без LLM» на деплое:** в панели Render → **Environment** добавьте **`GEMINI_API_KEY`** (или **`OPENAI_API_KEY`**) и сделайте redeploy. Локальный `backend/.env` на сервер не подставляется.

**Документы в БД, но не открываются / пустой текст на сервере:** в таблице `documents` не должно быть абсолютных путей вида `/Users/...` — на Render таких каталогов нет. Новые версии кода сохраняют **`data/uploads/...`**. После обновления зайдите под **админом** и один раз вызовите **`POST /api/admin/reindex-documents`** (или через curl с Bearer-токеном) — пути в БД подправятся, чанки пересчитаются, если файлы с тем же именем лежат в `UPLOAD_DIR` контейнера. Если базу переносили с ноутбука без файлов из `data/uploads`, загрузите документы заново или скопируйте файлы в том деплоя.

### Вариант A2 — Railway

Репозиторий с корнем **`edda-portal`** (где лежат `Dockerfile` и `railway.toml`) → **Deploy**. В **Variables** задайте сильный **`JWT_SECRET`**. Платформа подставляет **`PORT`** — образ уже слушает `${PORT:-8000}`.

### Вариант A3 — свой VPS / железо (полный стек без Postgres)

Из каталога `edda-portal`:

```bash
export JWT_SECRET="$(openssl rand -hex 32)"
docker compose -f docker-compose.prod.yml up -d --build
```

Откройте **http://127.0.0.1:8000**. Данные SQLite и загрузки — в Docker-томе `edda_app_data`. Перед **своим доменом** с HTTPS: поставьте nginx и используйте **`deploy/nginx-full-proxy-to-uvicorn.conf`** (весь трафик на Uvicorn), либо убедитесь, что в split-конфиге **`location /api/`** уходит на бэкенд — см. **`deploy/nginx-example.conf`**.

### Вариант B — остаться на Vercel для фронта

Нужны **два** URL: отдельно поднять API (тот же Docker на Render/Railway), в **Vercel** в переменных окружения задать **`VITE_API_BASE_URL`** = адрес API, пересобрать деплой; на бэкенде — **CORS** на домен Vercel (см. раздел «Деплой» ниже).

---

## Запуск без Docker (рекомендуется)

Нужны **Python 3.9+** и установленные зависимости (первый запуск подкачает модель эмбеддингов — может занять несколько минут).

```bash
cd edda-portal/backend
chmod +x run.sh
./run.sh
```

Откройте в браузере: **http://127.0.0.1:8000** (или http://localhost:8000)

Если порт «не открывается», убедитесь, что в терминале видно строку вроде `Uvicorn running on http://127.0.0.1:8000`, и подождите окончания первого старта (загрузка модели `sentence-transformers`).

**Демо-вход после первого запуска:**

| Email | Пароль | Роль |
|-------|--------|------|
| admin@edda.local | Admin123! | Администратор |
| it@edda.local | It123456! | ИТ (загрузка документов) |
| prod@edda.local | Prod123456! | Производство |
| hr@edda.local | Hr123456! | Кадры |

Пароль администратора при первом создании БД: переменная `SEED_ADMIN_PASSWORD` (иначе используется `Admin123!`).

**Не пускает в систему после обновления кода:** остановите сервер, удалите файл `backend/data/app.db` и запустите снова — база и демо-пользователи создадутся заново.

Демо-логины вида `*@edda.local` раньше отвергались проверкой `EmailStr` (домен `.local` считается служебным). Сейчас вход принимает такие адреса; перезапустите backend после обновления файлов.

---

## Только интерфейс разработки (Vite на порту 5173)

```bash
cd edda-portal/frontend
npm install
npm run dev
```

Сайт: http://localhost:5173 — запросы к API проксируются на тот же backend (порт 8000 должен быть запущен).

---

## Запуск с Docker (если установлен)

```bash
cd edda-portal
docker compose up --build
```

Откройте **http://localhost:8000**. В `docker-compose` по умолчанию используется PostgreSQL; для обычной работы приложению достаточно колонки JSON с векторами (расширение pgvector не требуется).

Переменная **`DATABASE_URL`**: для Postgres задайте `postgresql+psycopg://...`; если не задана — в коде по умолчанию SQLite (см. `backend/app/config.py`).

---

## Ответы ИИ (подключение LLM)

Поиск по базе (**RAG**) работает всегда: из документов, доступных вашей роли, выбираются фрагменты. **Связный ответ** даёт языковая модель, если она настроена; иначе в чате показываются **только вставки фрагментов** (режим «без LLM»).

1. Создайте файл **`edda-portal/backend/.env`** (образец: **`backend/.env.example`**).
2. Выберите один из вариантов (если задано несколько ключей, приоритет: **Gemini → OpenAI → Ollama**).

**Google Gemini** — ключ в [Google AI Studio](https://aistudio.google.com/apikey), в `.env`:

```env
GEMINI_API_KEY=...
GEMINI_MODEL=gemini-2.5-flash
```

Если для выбранной модели закончилась квота (HTTP 429) или имя устарело (404), backend автоматически пробует запасные варианты (`gemini-2.5-flash-lite`, `gemini-flash-latest`). При необходимости задайте другое имя вручную по [списку моделей](https://ai.google.dev/gemini-api/docs/models).

**OpenAI (или совместимый провайдер)** — в `.env`:

```env
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
```

**Локальная Ollama** — установите [Ollama](https://ollama.com), в терминале:

```bash
ollama pull llama3.2
ollama serve
```

В `.env` при необходимости укажите модель и адрес (по умолчанию `http://127.0.0.1:11434` и `llama3.2`):

```env
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
```

Если backend в **Docker**, а Ollama на компьютере хоста (macOS/Windows), для подключения к Ollama с контейнера задайте, например, `OLLAMA_BASE_URL=http://host.docker.internal:11434` (в Linux может понадобиться другой адрес хоста).

Файл **`backend/.env`** подхватывается **всегда по абсолютному пути** (не зависит от того, из какой папки вы запустили `uvicorn`). После правок **сохраните файл на диск** (Cmd+S / Ctrl+S): если на вкладке редактора есть **точка** у имени файла — изменения ещё не записаны, backend их не видит. Настройки перечитываются при запросах; если режим LLM не обновился — **перезапустите** backend.

Для **Gemini** ключ из Google AI Studio всегда начинается с **`AIzaSy`** (латинская «A» в начале). Если скопировалось без первой буквы, API не сработает.

На странице чата отображается, какой режим активен (Gemini / OpenAI / Ollama / только фрагменты). Ошибки API смотрите в логе консоли сервера.

---

## Деплой

Сборка фронта в статику и один процесс Uvicorn, либо образ из `Dockerfile`. В продакшене задайте сильный **`JWT_SECRET`**, HTTPS и резервное копирование БД.

**Если на сервере снова «Failed to fetch» при входе (а локально всё ок):**

1. **Один домен и nginx** — либо проксируйте **всё** на Uvicorn: `deploy/nginx-full-proxy-to-uvicorn.conf` (рекомендуется при деплое из корневого `Dockerfile`). Либо, если статика лежит на диске отдельно от API, запросы к **`/api/`** обязаны уходить на Uvicorn — пример: `deploy/nginx-example.conf`. В логах при старте API видно строку `CORS: N origin(s)` — проверьте, что `N` не 0 и в списке есть ваш `https://…`.
2. **Фронт и API на разных URL** — при сборке: `VITE_API_BASE_URL=https://api.ваш-домен.ru` или в `index.html` мета-тег `<meta name="edda-api-base" content="https://api...." />` / `window.__EDDA_API_BASE__`.
3. **Подкаталог сайта** (`https://host.ru/app/`) — в `frontend/vite.config.ts` задайте `base: '/app/'`, пересоберите фронт; либо укажите полный URL API через `VITE_API_BASE_URL`.
4. **CORS** — `CORS_ORIGINS=https://точный-url-фронта` (схема и домен как в адресной строке). Превью: `CORS_ORIGIN_REGEX` в `backend/.env.example`.
5. **HTTPS** — страница по `https://` не может вызывать API по `http://`.

### Vercel (статический фронт)

На Vercel **нет вашего FastAPI**: запрос на `…vercel.app/api/...` даёт **404** от Vercel — это нормально, пока не настроен адрес API.

1. Разверните бэкенд отдельно (Railway, Render, Fly.io, свой VPS + Docker и т.д.) и получите публичный `https://…` к Uvicorn.
2. В **Vercel → Project → Settings → Environment Variables** добавьте **`VITE_API_BASE_URL`** = базовый URL бэкенда **без** завершающего `/` (например `https://exxssaaee-api.up.railway.app`).
3. **Redeploy** проекта (переменные `VITE_*` подставляются на этапе `npm run build`).
4. На бэкенде в `.env`: **`CORS_ORIGINS`** = точный URL фронта на Vercel, либо **`CORS_ORIGIN_REGEX`** = `^https://.*\.vercel\.app$` (для превью-деплоев с разными поддоменами).

Проверка: в Network запрос к логину должен уходить на **хост API**, а не на `*.vercel.app`, если используете `VITE_API_BASE_URL`.
